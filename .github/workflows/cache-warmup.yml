name: Cache Warmup

on:
  schedule:
    # Every 6 hours — keeps the Notion guest cache and Netlify function warm.
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  warm:
    name: Warm guest cache
    runs-on: ubuntu-latest

    steps:
      - name: Warm cache
        env:
          ADMIN_SECRET: ${{ secrets.RESEND_ADMIN_SECRET }}
        run: |
          URL="https://sargaux.com/api/warm"
          echo "Warming cache at: ${URL}"
          if [ -z "${ADMIN_SECRET}" ]; then
            echo "Error: RESEND_ADMIN_SECRET GitHub secret is not set"
            exit 1
          fi
          RESPONSE=$(curl -s -w "\n%{http_code}" \
            -H "Authorization: Bearer ${ADMIN_SECRET}" \
            "${URL}")
          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -1)
          echo "Response: ${BODY}"
          echo "Status: ${HTTP_CODE}"
          if [ "${HTTP_CODE}" -ne 200 ]; then
            echo "Warning: warmup returned ${HTTP_CODE} (non-fatal)"
          fi

      - name: Check calendar health
        run: |
          URL="https://sargaux.com/api/calendar/health"
          echo "Checking calendar health at: ${URL}"
          RESPONSE=$(curl -s -w "\n%{http_code}" "${URL}")
          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -1)
          echo "Response: ${BODY}"
          echo "Status: ${HTTP_CODE}"
          if [ "${HTTP_CODE}" -ne 200 ]; then
            echo "Error: calendar health check failed with status ${HTTP_CODE}"
            echo "CALENDAR_HMAC_SECRET or NOTION_API_KEY may be missing from Netlify env vars"
            exit 1
          fi
          # Health body must not leak config flags
          if echo "$BODY" | grep -qE 'hasSecret|hasNotionKey|blobsOk'; then
            echo "Error: health endpoint leaked config flags"
            exit 1
          fi
